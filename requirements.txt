# Core dependencies for ingest_rag
click>=8.0
qdrant-client>=1.4
openai>=0.28
tqdm>=4.0
# HTML parsing and fetching
beautifulsoup4>=4.9
requests>=2.0
# Environment variable support
python-dotenv>=0.19
# Document ingestion
# (docling chunking via CLI only; not required for slash inject)
# docling<2.0
# Fallback core library for docling (provided automatically by docling<2.0 dependency)
# docling-core>=2.27

# Development and testing
pytest>=6.0
flask
